function [bestDisparity, bestCost] = bestDisparity(leftImage, rightImage, disparity)

  % Find the window size
  windowSize = size(leftImage, 1);

  % Create the absolute difference matrix
  

  bestDisparity = 0;
  maxDisparity = 

  for disparityIterator = 0 : maxDisparity,
    
  end

end